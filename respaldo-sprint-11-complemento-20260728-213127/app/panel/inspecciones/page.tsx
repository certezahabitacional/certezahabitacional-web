import Link from "next/link";import { prisma } from "@/lib/prisma";
export default async function Page(){const xs=await prisma.inspeccion.findMany({include:{cliente:true,inmueble:true,inspector:{include:{usuario:true}},_count:{select:{hallazgos:true}}},orderBy:{fechaProgramada:"desc"}});return <main className="min-h-screen bg-slate-950 px-6 py-8 text-white"><div className="mx-auto max-w-7xl"><div className="flex items-end justify-between"><div><Link href="/panel" className="text-sm font-bold text-cyan-300">← Panel</Link><h1 className="mt-2 text-3xl font-black">Inspecciones</h1></div><Link href="/panel/inspecciones/nueva" className="rounded-full bg-cyan-400 px-5 py-3 font-black text-slate-950">Nueva inspección</Link></div><section className="mt-8 overflow-hidden rounded-3xl border border-white/10 bg-slate-900">{xs.length===0?<p className="p-14 text-center text-slate-400">No hay inspecciones registradas.</p>:<div className="divide-y divide-white/10">{xs.map(i=><Link key={i.id} href={`/panel/inspecciones/${i.id}`} className="grid gap-4 p-6 hover:bg-white/[0.03] md:grid-cols-[1fr_1fr_1fr_auto]"><div><p className="font-black text-cyan-300">{i.folio}</p><p className="text-sm text-slate-400">{i.cliente.nombre}</p></div><div><p className="font-bold">{i.inmueble?.alias??i.tipoInmueble}</p><p className="text-sm text-slate-400">{i.ciudad}</p></div><div><p className="font-bold">{i.inspector?.usuario.nombre??"Sin inspector"}</p><p className="text-sm text-slate-400">{i._count.hallazgos} hallazgo(s)</p></div><span className="h-fit rounded-full bg-white/5 px-3 py-2 text-xs font-black">{i.estado.replaceAll("_"," ")}</span></Link>)}</div>}</section></div></main>}
